# Pointer

- Pointer store the **address** of an object
- type of a Pointer is `Foo*`

value of a pointer

dereferenced symbol `*`

```cpp
int number = 42;
int* ptr = &number; // declare a ptr that holds the address of number
int value = *ptr; // value is now 42, dereferencing to get the value at that address
```
