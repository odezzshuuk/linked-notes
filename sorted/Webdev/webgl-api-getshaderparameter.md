# webgl api getShaderParameter

## Syntax

```js
glInstance.getShaderParameter(shader, pname);
```

- `pname`: the parameter to check
  - gl.COMPILE_STATUS
  - gl.DELETE_STATUS
  - gl.LINK_STATUS
- return the `pname`
